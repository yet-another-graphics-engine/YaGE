#include "shape_property.h"
#include "color.h"

namespace yage {
namespace draw {

ShapeProperty::ShapeProperty() : fgcolor(0, 0, 0, 1), bgcolor(1, 1, 1, 1), thickness(1) {}

} // namespace draw
} // namespace yage
